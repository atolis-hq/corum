name: ReadModel
info:
  version: "1.0.0"
  core: false
  abstract: false
  description: |
    A derived query projection optimised for reads. It captures a view of
    domain data shaped for consumption rather than mutation.

edges:
  incoming:
    - reads
    - derived-from

ui:
  icon: table-list
  colour: "#6C91BF"
  displayProperties: []
