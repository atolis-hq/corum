name: Command
info:
  version: "1.0.0"
  core: false
  abstract: false
  description: |
    An explicit intent to change state. Useful when a team models commands
    distinctly from transport concerns such as HTTP endpoints.

edges:
  outgoing:
    - triggers
  incoming:
    - produces

ui:
  icon: terminal
  colour: "#B56576"
  displayName: Command
  displayProperties: []
