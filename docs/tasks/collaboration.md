this will be a paid feature eventually.

collaboration cannot be done via git, it needs a hosted service.

Users log in, and can comment on any node - eg a field or a api endpoint ( maybe target properties? ). 
Comments are threads, similar to how confluence works.

Comment status is shown on nodes and discussion threads pop up in a sidebar.

Users can see open discussions on the dashboard.

Email / notification hooks are used to inform the user of comments.