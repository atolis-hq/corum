ability to define user journeys and plot out a event model like swimlane.

use official language where possible.

journey made up of steps / stages. (columns)

swimlanes per component.

clusters of nodes and related nodes together (features / stories?)
lines between clusters show relationships / triggers / calls. 
laid out in time left to right

Ability to filter / collapse components/swimlanes.

Multiple journeys could reference the same stories.

This should feel related to event modelling.


stage 2. edit mode, ability to build out a model starting with the nodes / bdd. maybe a sidebar editor to specify steps, eg api -> aggregate -> domain event -> integration event ----> event handler -> read model

Needs some thought.