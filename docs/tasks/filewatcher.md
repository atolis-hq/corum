when the server is running in file mode. add an option to use a file watcher.

When the graph yamls or the template yamls change, then update the graph. either a partial or full reload. 

ideally, the ui should also auto update - as long as it doesnt make things super complex.