use a tool such as treesitter to extract internal schemas from code (any language) and populate the graph. This is more deterministic vs using an agent.

We my need to be able to scope the search to specific locations