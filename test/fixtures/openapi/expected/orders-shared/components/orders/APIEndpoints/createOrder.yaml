id: orders.APIEndpoint.createOrder
template: APIEndpoint
schemaVersion: '1'
metadata:
  component: orders
  state: implemented
  stability: unstable
  lastModifiedAt: <date>
  extractedFrom: <spec>
  derivation: determined
  derivedBy: adapter:openapi
properties:
  method: POST
  path: /orders/create
  request: '#/schemas/createOrder-request'
  responses:
    '201': orders.Schema.OrderSummary
schemas:
  createOrder-request:
    fields:
      customerId:
        type: uuid
        nullable: true
        cardinality: one
