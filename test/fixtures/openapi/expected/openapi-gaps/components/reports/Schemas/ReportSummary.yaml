id: reports.Schema.ReportSummary
template: Schema
schemaVersion: '1'
metadata:
  component: reports
  state: implemented
  stability: unstable
  lastModifiedAt: <date>
  extractedFrom: <spec>
  derivation: determined
  derivedBy: adapter:openapi
fields:
  groupedMetrics:
    $ref: '#/mappings/groupedMetrics'
    nullable: true
  labelsByGroup:
    $ref: '#/mappings/labelsByGroup'
    nullable: true
  location:
    type: string
    nullable: true
  metricList:
    $ref: reports.Schema.Metric
    nullable: true
    collection: array
  metricsByCategory:
    $ref: '#/mappings/metricsByCategory'
    nullable: true
  nestedMetrics:
    $ref: '#/mappings/nestedMetrics'
    nullable: true
  perPersonBreakdown:
    $ref: '#/mappings/perPersonBreakdown'
    nullable: true
  primaryMetric:
    type: string
    nullable: true
mappings:
  groupedMetrics:
    value-collection: array
    $ref: reports.Schema.Metric
  labelsByGroup:
    value-collection: array
    type: string
  metricsByCategory:
    $ref: reports.Schema.Metric
  nestedMetrics:
    $ref: reports.Schema.ReportSummary.mappings.nestedMetrics-values
  nestedMetrics-values:
    $ref: reports.Schema.Metric
  perPersonBreakdown:
    $ref: reports.Schema.ReportSummary.mappings.perPersonBreakdown-values
  perPersonBreakdown-values:
    type: integer
