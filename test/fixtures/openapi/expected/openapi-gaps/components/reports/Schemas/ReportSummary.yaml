id: reports.Schema.ReportSummary
template: Schema
schemaVersion: '1'
metadata:
  component: reports
  state: implemented
  stability: unstable
  lastModifiedAt: <date>
  extractedFrom: <spec>
  derivation: determined
  derivedBy: adapter:openapi
fields:
  groupedMetrics:
    $ref: '#/mappings/groupedMetrics'
    nullable: true
  labelsByGroup:
    $ref: '#/mappings/labelsByGroup'
    nullable: true
  location:
    type: string
    nullable: true
  metricList:
    $ref: reports.Schema.Metric
    nullable: true
    collection: array
  metricsByCategory:
    $ref: '#/mappings/metricsByCategory'
    nullable: true
  nestedMetrics:
    $ref: '#/mappings/nestedMetrics'
    nullable: true
  perPersonBreakdown:
    $ref: '#/mappings/perPersonBreakdown'
    nullable: true
  primaryMetric:
    type: string
    nullable: true
mappings:
  groupedMetrics:
    value-collection: array
    value-ref: reports.Schema.Metric
  labelsByGroup:
    value-collection: array
    value-type: string
  metricsByCategory:
    value-ref: reports.Schema.Metric
  nestedMetrics:
    value-ref: reports.Schema.ReportSummary.mappings.nestedMetrics-values
  nestedMetrics-values:
    value-ref: reports.Schema.Metric
  perPersonBreakdown:
    value-ref: reports.Schema.ReportSummary.mappings.perPersonBreakdown-values
  perPersonBreakdown-values:
    value-type: integer
