id: payments.APIEndpoint.capturePayment
template: APIEndpoint
schemaVersion: '1'
metadata:
  component: payments
  state: implemented
  stability: unstable
  lastModifiedAt: <date>
  extractedFrom: <spec>
  derivation: determined
  derivedBy: adapter:openapi
properties:
  method: POST
  path: /payments/capture
  request: '#/schemas/capturePayment-request'
  responses:
    '200': '#/schemas/capturePayment-response-200'
schemas:
  capturePayment-request:
    fields:
      amount:
        type: decimal
        nullable: false
      orderId:
        type: uuid
        nullable: false
  capturePayment-response-200:
    fields:
      paymentId:
        type: uuid
        nullable: false
