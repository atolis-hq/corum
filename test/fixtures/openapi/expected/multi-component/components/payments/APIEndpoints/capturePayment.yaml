id: payments.APIEndpoint.capturePayment
template: APIEndpoint
schemaVersion: '1'
metadata:
  component: payments
  state: implemented
  stability: unstable
  lastModifiedAt: <date>
  extractedFrom: <spec>
  derivation: determined
  derivedBy: adapter:openapi
properties:
  method: POST
  path: /payments/capture
  operationId: capturePayment
schemas:
  capturePayment-request:
    fields:
      amount:
        type: decimal
        nullable: false
        cardinality: one
      orderId:
        type: uuid
        nullable: false
        cardinality: one
  capturePayment-response-200:
    fields:
      paymentId:
        type: uuid
        nullable: false
        cardinality: one
